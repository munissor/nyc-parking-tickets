## Exports the dataset from the DB to a file

## Configuring
Make sure .`/config/default.json` points to a valid database

## Load dependencies
`npm install`

### Usage
`./export [fromid] [toid] [file]`
